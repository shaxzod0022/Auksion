export const metadata = {
  title: "Mening Profilim | Universal Auksion Invest",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProfileLayout({ children }) {
  return children;
}
