export const metadata = {
  title: "Tizimga kirish | Universal Auksion Invest",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return children;
}
